{
  "name": "blackjack",
  "version": "1.0.0",
  "description": "Browser-based Blackjack game with split, betting, sound effects, and card style toggle",
  "type": "module",
  "main": "src/main.js",
  "scripts": {
    "start": "npx serve . -l 3000",
    "test": "npx playwright test",
    "test:e2e": "npx playwright test tests/e2e/",
    "test:unit": "node --test tests/unit/hand.test.js",
    "test:headed": "npx playwright test --headed",
    "test:ui": "npx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
